// Print the sum of all digits in a number using for loop
#include<stdio.h>
int main(){
    int x,n,sum=0;
    scanf("%d",&x);
    for(;x>0;){
        n=x%10;
        sum=sum+n;
        x=x/10;
    }
    printf("%d",sum);
    return 0;
}
