// Print the sum of two digit numbers whose one's digits is 5 using for loop
#include<stdio.h>
int main(){
    int sum=0;
    for(int x=10;x<=99;x++){
        if(x%10==5)
        sum=sum+x;
    }
    printf("%d",sum);
    return 0;
}
