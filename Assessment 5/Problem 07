// Print two digit odd numbers whose sum of digits is 7 using for loop
#include<stdio.h>
int main(){
    for(int x=10;x<=99;x++){
        if(x%2==1 && x%10+x/10==7)
        printf("%d\n",x);
    }
    return 0;
}
