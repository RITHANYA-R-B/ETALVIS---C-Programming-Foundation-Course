// Print whether prime or not using for loop
#include<stdio.h>
int main(){
    int x,n=0;
    scanf("%d",&x);
    for(int i=2;i<=x/2;i++){
        if(x%i==0)
            n++;
    }
    if((n==0 && x>1)||(x==2||x==3))
    printf("Prime");
    else
    printf("Not Prime");
    return 0;
}
