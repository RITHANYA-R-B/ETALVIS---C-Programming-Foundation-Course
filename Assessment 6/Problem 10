// Print the sum of two digit odd numbers whose ten's digits is 7 using while loop
#include<stdio.h>
int main(){
    int x=10,sum=0;
    while(x<100){
        if(x%2==1 && x/10==7)
            sum=sum+x;
        x++;
    }
    printf("%d",sum);
    return 0;
}
