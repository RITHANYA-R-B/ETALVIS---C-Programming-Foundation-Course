// Print two digit even numbers whose sum of digits is 6 using while loop
#include<stdio.h>
int main(){
    int x=10;
    while(x<100){
        if(x%2==0 && x%10+x/10==6)
         printf("%d\n",x);
        x++;
    }
    return 0;
}
