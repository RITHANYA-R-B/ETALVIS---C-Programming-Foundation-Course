// Print two digit odd numbers whose sum of digits is 7 using while loop
#include<stdio.h>
int main()
{
    int x=10;
    while(x<100){
        if(x%2==1 && x%10+x/10==7)
         printf("%d\n",x);
        x++;
    }
    return 0;
}
