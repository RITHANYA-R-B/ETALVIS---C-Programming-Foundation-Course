// Check if the first 2 digits and the last 2 digits are equal or not in a four digit number using if-else
#include<stdio.h>
int main()
{
    int x;
    printf("Enter Number: ");
    scanf("%d",&x);
    if(x%100==x/100)
    printf("Success");
    else
    printf("Failure");
    return 0;
}
