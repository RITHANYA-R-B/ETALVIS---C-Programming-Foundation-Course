// Check if a two digit number's digits are identical using if-else
#include<stdio.h>
int main()
{
    int x;
    printf("Enter Number: ");
    scanf("%d",&x);
    if(x%10==x/10)
    printf("Success");
    else
    printf("Failure");
    return 0;
}
