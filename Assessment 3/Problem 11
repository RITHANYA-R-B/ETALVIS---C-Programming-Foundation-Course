// Check if ten's digit is less than or equal to one's digit in a two digit number using if-else
#include<stdio.h>
int main()
{
    int x;
    printf("Enter Number: ");
    scanf("%d",&x);
    if(x%10>=x/10)
    printf("Success");
    else
    printf("Failure");
    return 0;
}
