// Check if the one's digit and ten's digit are equal or not in a four digit number using if-else
#include<stdio.h>
int main()
{
    int x;
    printf("Enter Number: ");
    scanf("%d",&x);
    if(x%10<=(x/10)%10)
    printf("Success");
    else
    printf("Failure");
    return 0;
}
