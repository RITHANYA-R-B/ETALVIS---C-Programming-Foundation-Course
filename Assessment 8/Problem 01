// Print sum of 1 to 5 using function (with return value)
#include<stdio.h>
int disp_sum();
int main(){
    int y=disp_sum();
    printf("%d",y);
    return 0;
}
int disp_sum(){
    int y=0;
    for(int i=1;i<=5;i++)
        y=y+i;
    return y;
}
