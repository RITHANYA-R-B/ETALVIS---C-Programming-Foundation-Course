// Get a two digit number and check if the ten's digit is less than or equal to the one's digit
#include<stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=x%10>=x/10;
    printf("Result = %d",y);
    return 0;
}
