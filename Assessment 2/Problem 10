// Get a two digit number and check if the one's digit is greater than the ten's digit
#include<stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=x%10>x/10;
    printf("Result = %d",y);
    return 0;
}
