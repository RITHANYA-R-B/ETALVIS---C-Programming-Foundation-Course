//Get a three digit number and print the sum of digits
#include <stdio.h>
int main()
{
    int x,y=0;
    printf("Enter Number: ");
    scanf("%d",&x);
    while(x>0){
    y=y+(x%10);
    x=x/10;
    }
    printf("Result = %d",y);
    return 0;
}
