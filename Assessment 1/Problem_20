//Get a three digit number and make the ten's digit as 0
#include <stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=(x/100)*100+x%10;
    // OR y=x-(10*((x/10)%10));
    printf("Result = %d",y);
    return 0;
}
