//Get a number and subtract 5 if it's ten's position digit is odd
#include <stdio.h>
int main()
{
    int x,y=0;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=(x/10)%10;
    y%2==0?printf("Result = %d",x):printf("Result = %d",x-5);
    return 0;
}
