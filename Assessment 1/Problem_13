//Get a two digit number and print the reverse of it
#include <stdio.h>
int main()
{
    int x,y=0;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=((x%10)*10)+(x/10);
    printf("Result = %d",y);
    return 0;
}
