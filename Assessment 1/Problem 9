//Get a three digit number and print the hundred's digit
#include <stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=x/100;
    printf("Result = %d",y);
    return 0;
}
