//Get a number, divide it by 8 and print the remainder
#include <stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=x%8;
    printf("Result = %d",y);
    return 0;
}
