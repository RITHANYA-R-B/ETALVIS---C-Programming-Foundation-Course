//Get a number, divide it by 6 and print the result
#include <stdio.h>
int main()
{
    int x,y;
    printf("Enter Number: ");
    scanf("%d",&x);
    y=x/6;
    printf("Result = %d",y);
    return 0;
}
