// Get a number and print the total number of digts in it (using functions)
#include<stdio.h>
void count_total_digits(int x);
int main(){
    int x;
    scanf("%d",&x);
    count_total_digits(x);
    return 0;
}
void count_total_digits(int x){
    int n=0;
    if(x==0)
        n=1;
    else
        for(;x>0;x=x/10)
            n++;
    printf("%d",n);
}
