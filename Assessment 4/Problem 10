// Print the sum of two digit odd numbers whose ten's digit is 7.
#include<stdio.h>
int main()
{
    int x=10,sum=0;
    loop: if(x<100){
        if(x%2!=0){
            if(x/10==7)
            sum=sum+x;
        }
        x=x+1;
        goto loop;
    }
    printf("%d",sum);
    return 0;
}
